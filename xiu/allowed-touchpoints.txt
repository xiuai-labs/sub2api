# Upstream files this fork edits. One line per path, `#` starts a comment.
#
# The gate (check-touchpoints.sh) reads this in BOTH directions: an undeclared edit fails,
# and so does a declared-but-unmodified entry. Declare a file when the change lands, never
# before — an entry with no diff behind it is a standing permission nobody reviewed.
#
# Every entry here is merge risk we pay for on each upstream rebase, so the list should
# shrink, not grow. Each one needs a row in PATCHES.md saying why it cannot live in xiu/.

# 新增文件，刻意放在仓库根 —— 补丁台账要让人在读任何代码之前先看见。
PATCHES.md

# ── 长上下文缺 credits 不封整号：429 分类 / 调度 key / 进门估长度，各一处挂载（见 PATCHES.md）──
backend/internal/handler/gateway_handler.go
backend/internal/service/model_rate_limit.go
backend/internal/service/ratelimit_service.go
